{
  "name": "dm-preview",
  "version": "0.1.0",
  "description": "Preview MapLibre output generated by dm-converter",
  "type": "module",
  "bin": {
    "dm-preview": "./bin/dm-preview.js"
  },
  "files": [
    "assets",
    "bin",
    "maplibre",
    "src",
    "LICENSE",
    "THIRD_PARTY_LICENSES"
  ],
  "scripts": {
    "check": "node --check bin/dm-preview.js && node --check src/asset-roots.js && node --check src/bundle.js && node --check src/main.js && node --check src/manifest.js && node --check src/open-browser.js && node --check src/server.js && node --check src/gpkg-features.js && node --check assets/app.js && node --check scripts/generate-maplibre-assets.mjs",
    "test": "node --test",
    "generate:maplibre-assets": "node scripts/generate-maplibre-assets.mjs",
    "build": "bun build --compile bin/dm-preview.js --outfile dist/dm-preview && node scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "maplibre-gl": "5.24.0",
    "pmtiles": "4.3.0",
    "proj4": "^2.20.9",
    "sql.js": "^1.14.1"
  }
}
